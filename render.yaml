services:
  - type: web
    name: news-hub
    env: node
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /api/news
    envVars:
      - key: NODE_ENV
        value: production
